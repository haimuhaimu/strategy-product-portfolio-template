# 路线图

<!-- markdownlint-disable MD024 -->

本路线图以社区采用情况而非日期为门槛。Star 是便于观察的公开信号，不等同于质量、用户数或交付承诺；维护者会结合真实使用反馈调整顺序。每一阶段都先完成隐私与可移植性底线，再扩展功能。

## 当前主线：Skill-first

当前主线是用 `portfolio-story-builder` 完成“简历 / 经历 → 单问题追问 → 3 个案例 → 证据 / 隐私审计 → 可投递网站”。网站模板是 Skill 的静态输出载体；`/config/` 编辑器保留为不使用 Agent 时的可视化 fallback。首次使用仍严格收缩为 3 个案例和必要首屏，不把高级个人模型并入主线。

当前只验证产品与运营两条叙事、v2 数据契约、审计和静态构建，不引入账号、后端、托管式编辑器或更多角色范围。

## 30 Star：让第一批真实作品安全上线

### 产品目标

- 帮助产品经理、产品运营和策略/增长运营从简历与经历出发，经 Skill 追问、三案例精选和审计后完成可公开访问的作品集。
- 形成第一批经作者明确同意展示的真实用户作品 Showcase，让后来者看到不同角色如何组织证据链，而不是只看示例数据。
- 验证 `product`、`operations` 两条 Skill 叙事与 v2 输出边界；编辑器只承担可视化 fallback。
- 把证据质量和隐私安全作为生成与发布流程的一部分，而不是发布后的补救措施。

### 可验证指标

- 收录至少 3 个作者主动提交、可公开访问且经隐私确认的 Showcase；记录所用 preset、改造重点和可复用经验。
- 至少 5 位非维护者完成初始化，其中至少 3 位反馈可在 30 分钟内启动本地预览。
- `product` 与 `operations` 两条路径持续通过 Skill 审计、`npm run test:init`、`npm run test:public` 和静态构建。
- 建立可执行的 Showcase 审核清单；收录条目中不出现内部数据、私人联系方式、未授权素材或精确敏感业务指标。
- 关闭或明确分流首批高优先级安装、初始化和移动端展示问题。

### 工程工作

- 保持仓库 Skill 镜像、非 strict 骨架审计和 v2 数据契约可验证，并补齐 product / operations 主线的失败提示。
- 加强公开安全测试与贡献模板中的隐私检查，覆盖文本、图片元数据、环境文件和仓库历史风险。
- 修复阻塞 Skill 输出接入、可视化 fallback、静态导出、GitHub Pages 路径和响应式的问题。
- 为 Showcase 建立低冲突的数据/文档承载方式；未确定稳定结构前不把外部内容直接写入首页高冲突区域。

### 社区动作

- 通过 Showcase Issue 征集真实作品，只接受作者本人或获明确授权的提交。
- 邀请产品与运营两类首批用户分别走通初始化、内容替换、隐私检查和部署，并公开可复用的非敏感结论。
- 将适合首次贡献的问题标注清楚，优先提供 preset 文案、匿名示例、无障碍和跨平台验证任务。

### 不做什么

- 不收集用户原始简历、公司文档、客户/用户数据或未公开业务材料。
- 不建立账号系统、后端数据库、托管式作品集平台或访问分析后台。
- 不为增加数量而收录未经授权、无法访问或隐私边界不清的 Showcase。
- 不一次性扩展大量角色 preset；先验证产品与运营两条主路径。

## 100 Star：把个性化能力沉淀为可复用模块

### 产品目标

- 让社区可独立贡献质量一致的角色 preset、个人模型组件和主题。
- 在保持纯静态导出与数据驱动结构的前提下，降低深度定制成本。
- 让作品集在键盘操作、移动端阅读、SEO 和主流部署环境中具备稳定基线。

### 可验证指标

- 合入至少 3 个由非维护者主导的可复用贡献，且覆盖 preset、个人模型组件、主题中的至少两类。
- 至少 10 个公开 Showcase 使用两种以上角色定位或主题组合，并全部完成提交者隐私确认。
- 所有合入 PR 在 Ubuntu 与 Windows CI 上通过初始化、个人模型、认知校准、公开安全、lint、静态构建和 SEO 检查。
- 对核心页面完成一次键盘可达性、语义结构和移动端检查，高优先级问题有测试或复现记录。
- 新增 preset 或主题均具备初始化回归测试、可辨识名称和安全示例内容。

### 工程工作

- 评估并实现稳定的 preset 注册/校验约定，减少在初始化脚本中散落分支。
- 梳理个人模型展示组件的输入边界、空状态和长文本行为，补足针对性测试。
- 将主题 token、初始化器主题选项和视觉回归说明保持同步，验证对比度与响应式状态。
- 完善静态导出、base path、sitemap、robots 和结构化数据的回归覆盖。
- 在不引入运行时服务的前提下改善错误提示、示例数据校验和贡献者开发体验。

### 社区动作

- 为 preset、个人模型组件和主题分别维护可复现的验收样例。
- 定期整理 Showcase 中出现的共性改造，只将多位用户验证过的模式纳入核心模板。
- 鼓励贡献者在 Issue 中先说明用户场景、数据边界和验证方法，减少大改后返工。
- 发布阶段复盘：哪些能力被真实采用、哪些仅有投票但没有使用证据。

### 不做什么

- 不引入插件市场、远程配置中心或需要登录的编辑器。
- 不承诺兼容任意自定义 JSON 结构；公共字段演进需要类型、迁移说明和测试。
- 不把单个作品的专属视觉直接并入默认主题。
- 不用遥测替代主动反馈，不默认植入第三方统计脚本。

## 1000 Star：形成稳定、可治理的作品集生态

### 产品目标

- 形成覆盖多种产品与运营角色、但仍可理解和维护的模板生态。
- 为 preset、主题和个人模型模块建立版本化兼容边界与升级路径。
- 让多语言、可访问性和长期内容维护成为核心能力，同时继续保持隐私优先、静态优先。

### 可验证指标

- 至少 50 个经作者授权的公开 Showcase，覆盖不少于 8 种角色/行业表达路径；定期检查失效链接和撤回请求。
- 至少 20 位非维护者贡献过已合入改动，至少 5 位持续参与评审、文档或社区支持。
- 发布版本具备变更记录、迁移说明和兼容性验证；严重回归有复现测试与修复复盘。
- 核心页面达到公开声明的可访问性基线，并由自动检查加人工抽查共同验证。
- preset/主题的弃用与升级有明确周期，不在无迁移说明时破坏已有作品集。

### 工程工作

- 设计轻量的 schema 版本与迁移机制，保证旧作品可预测升级。
- 在实际需求验证后支持多语言内容组织，并保持 SEO、路由和静态导出一致。
- 建立可维护的视觉/可访问性回归方案，以及 preset、主题组合的代表性测试矩阵。
- 优化构建体积和页面性能，公开基准条件，避免以单次本地数据宣称普遍收益。
- 完善发布、弃用、安全报告和维护者轮值流程，降低单点维护风险。

### 社区动作

- 建立透明的提案与决策记录；影响数据结构或兼容性的改动先讨论、后实现。
- 培养领域维护者分别协助 preset、主题、可访问性和文档评审。
- 定期维护 Showcase：尊重作者更新、匿名化和下架请求，不复制保存作者未授权内容。
- 用阶段指标和真实案例复盘路线图，公开推迟、放弃或调整项目的原因。

### 不做什么

- 不转型为招聘平台、社交网络、简历数据交易或集中托管服务。
- 不要求用户上传私密材料换取功能，也不建立跨站用户画像。
- 不以 preset 数量为目标牺牲维护质量，不无限扩张行业模板。
- 不牺牲静态部署、可导出性和数据所有权来换取平台锁定能力。

## 如何影响路线图

- 小范围改进或明确缺陷：使用对应 Issue 模板并附复现与验收方式。
- 新 preset、主题、数据结构或跨页面能力：先提交 Feature Request，说明目标用户、现有替代方案、隐私影响和最小范围。
- 已上线作品：使用 Showcase 模板，并确认链接、内容和素材均可公开。
- 路线图描述的是当前方向。是否进入某阶段以维护者公开确认及前置指标完成情况为准。
