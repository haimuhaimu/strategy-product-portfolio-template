# 安全与隐私政策

## 支持范围

本项目是纯静态作品集模板和 Agent Skill，不提供托管后端。安全问题主要包括依赖漏洞、静态导出导致的内容暴露、审计规则绕过，以及配置页在浏览器中的数据处理问题。

## 报告安全问题

请不要在公开 Issue 中提交密钥、Token、真实用户数据、内部链接或尚未披露的漏洞细节。可先通过仓库维护者的 GitHub 主页联系，并提供受影响版本、复现步骤、影响范围和建议修复方式。维护者确认安全披露方式后，再发送必要的最小信息。

普通功能缺陷、文案问题和不包含敏感信息的兼容性问题，可以直接使用公开 Issue。

## 使用者责任

配置页的草稿默认只保存在当前浏览器，但浏览器扩展、共享设备和本机恶意软件仍可能访问本地数据。不要直接导入未经脱敏的公司材料、客户信息、内部指标或个人敏感信息。

发布前请运行严格审计和公开安全测试：

```bash
python3 skills/portfolio-story-builder/scripts/audit_portfolio.py data/projects.json --strict --output audit-report.json
npm run test:public
npm run build
```

自动审计只能发现常见模式，不能替代所在组织的保密审查或本人对披露权限的确认。

## 依赖更新

发现依赖安全问题时，优先提交包含最小版本升级、回归测试和影响说明的修复。不得为修复单一漏洞无关地重写项目结构。
